# CurriculumLens Backend — Python Dependencies
# Tested on Python 3.11

# Web Framework
fastapi
uvicorn[standard]
python-multipart

# Database (PostgreSQL + pgvector)
sqlalchemy[asyncio]
asyncpg
pgvector

# Graph Database (Neo4j)
neo4j

# Document Processing (PDF reading)
# Note: using latest PyMuPDF — older pinned versions don't have Python 3.14 wheels
pymupdf

# LLM Communication (async HTTP calls to Ollama)
httpx

# Environment variable loading
python-dotenv
pydantic
