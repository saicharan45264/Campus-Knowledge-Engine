# ══════════════════════════════════════════════════════
# CurriculumLens Backend — Python Dependencies
# ══════════════════════════════════════════════════════

# ── Web Framework ──
fastapi==0.115.0
uvicorn[standard]==0.31.0
python-multipart==0.0.12

# ── Database (PostgreSQL & pgvector) ──
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.14.0
pydantic-settings==2.6.0
pgvector==0.3.6

# ── Graph Database (Neo4j) ──
neo4j==5.25.0

# ── Document Processing ──
pymupdf==1.24.0           # PDF parsing & page rendering
python-pptx==1.0.2         # PowerPoint parsing
pillow==11.0.0             # Image processing

# ── AI/ML (Core) ──
torch==2.5.0               # PyTorch for model inference
transformers==4.46.0        # HuggingFace model loading
sentence-transformers==3.3.0  # Embedding models (BGE-M3)

# ── LLM Integration ──
ollama==0.3.0              # Local LLM inference via Ollama
groq==0.11.0               # Groq API for fast inference
httpx==0.27.0              # Async HTTP client

# ── Text Processing ──
langchain-text-splitters==0.3.0  # Semantic text chunking
tiktoken==0.8.0            # Token counting

# ── Utilities ──
python-dotenv==1.0.1
pydantic==2.10.0
